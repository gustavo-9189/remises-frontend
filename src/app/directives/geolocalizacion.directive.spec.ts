import { GeolocalizacionDirective } from './geolocalizacion.directive';

describe('GeolocalizacionDirective', () => {
  it('should create an instance', () => {
    const directive = new GeolocalizacionDirective();
    expect(directive).toBeTruthy();
  });
});
